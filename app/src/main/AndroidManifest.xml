<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahul.covid19">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Corona_Tracker"></activity>
        <activity android:name=".meditate_three" />
        <activity android:name=".meditate_two" />
        <activity android:name=".meditate" />
        <activity android:name=".check_symptoms_two" />
        <activity android:name=".preventionScreenTwo" />
        <activity android:name=".preventionScreen" />
        <activity android:name=".symptoms_s" />
        <activity android:name=".check_symptoms" />
        <activity android:name=".symptoms_no" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>